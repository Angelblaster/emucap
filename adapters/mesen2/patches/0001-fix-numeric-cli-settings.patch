diff --git i/UI/Config/ConfigManager.cs w/UI/Config/ConfigManager.cs
index 44c3074..9412e2e 100644
--- i/UI/Config/ConfigManager.cs
+++ w/UI/Config/ConfigManager.cs
@@ -93,34 +93,33 @@ namespace Mesen.Config
 					return false;
 				}
 
-				if(t == typeof(int) || t == typeof(uint) || t == typeof(double)) {
-					if(property.GetCustomAttribute<MinMaxAttribute>() is MinMaxAttribute minMaxAttribute) {
-						if(t == typeof(int)) {
-							if(int.TryParse(value, out int result)) {
-								if(result >= (int)minMaxAttribute.Min && result <= (int)minMaxAttribute.Max) {
-									property.SetValue(instance, result);
-								} else {
-									return false;
-								}
-							}
-						} else if(t == typeof(uint)) {
-							if(uint.TryParse(value, out uint result)) {
-								if(result >= (uint)(int)minMaxAttribute.Min && result <= (uint)(int)minMaxAttribute.Max) {
-									property.SetValue(instance, result);
-								} else {
-									return false;
-								}
-							}
-						} else if(t == typeof(double)) {
-							if(double.TryParse(value, out double result)) {
-								if(result >= (double)minMaxAttribute.Min && result <= (double)minMaxAttribute.Max) {
-									property.SetValue(instance, result);
-								} else {
-									return false;
-								}
-							}
-						}
+				if(t == typeof(int)) {
+					if(!int.TryParse(value, out int result)) {
+						return false;
 					}
+					if(property.GetCustomAttribute<MinMaxAttribute>() is MinMaxAttribute minMaxAttribute &&
+						(result < (int)minMaxAttribute.Min || result > (int)minMaxAttribute.Max)) {
+						return false;
+					}
+					property.SetValue(instance, result);
+				} else if(t == typeof(uint)) {
+					if(!uint.TryParse(value, out uint result)) {
+						return false;
+					}
+					if(property.GetCustomAttribute<MinMaxAttribute>() is MinMaxAttribute minMaxAttribute &&
+						(result < (uint)(int)minMaxAttribute.Min || result > (uint)(int)minMaxAttribute.Max)) {
+						return false;
+					}
+					property.SetValue(instance, result);
+				} else if(t == typeof(double)) {
+					if(!double.TryParse(value, out double result)) {
+						return false;
+					}
+					if(property.GetCustomAttribute<MinMaxAttribute>() is MinMaxAttribute minMaxAttribute &&
+						(result < (double)minMaxAttribute.Min || result > (double)minMaxAttribute.Max)) {
+						return false;
+					}
+					property.SetValue(instance, result);
 				} else if(t == typeof(bool)) {
 					if(bool.TryParse(value, out bool boolValue)) {
 						property.SetValue(instance, boolValue);
diff --git i/UI/Program.cs w/UI/Program.cs
index 5033778..168e883 100644
--- i/UI/Program.cs
+++ w/UI/Program.cs
@@ -53,7 +53,11 @@ namespace Mesen
 
 			Environment.CurrentDirectory = ConfigManager.HomeFolder;
 
-			if(!File.Exists(ConfigManager.GetConfigFile())) {
+			//Apply config switches before the first-run and SingleInstance decisions. The main window
+			//processes the arguments again later to load files and Lua scripts.
+			_ = new CommandLineHelper(args, true);
+
+			if(!File.Exists(ConfigManager.GetConfigFile()) && !ConfigManager.DisableSaveSettings) {
 				//Could not find configuration file, show wizard
 				DependencyHelper.ExtractNativeDependencies(ConfigManager.HomeFolder);
 				App.ShowConfigWindow = true;
